{
  "name": "storage-lru",
  "version": "0.0.2",
  "description": "An LRU implementation that can be used with local storage, or other storage mechanisms that support a similar interface.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/yahoo/storage-lru.git"
  },
  "scripts": {
    "cover": "node node_modules/istanbul/lib/cli.js cover --dir artifacts -- ./node_modules/mocha/bin/_mocha tests/unit/ --recursive --reporter spec",
    "lint": "jshint lib tests",
    "test": "mocha tests/unit/ --recursive --reporter spec"
  },
  "author": "Lingyan Zhu <lingyan@yahoo-inc.com",
  "licenses": [
    {
      "type": "BSD",
      "url": "https://github.com/yahoo/storage-lru/blob/master/LICENSE.md"
    }
  ],
  "devDependencies": {
    "chai": "^1.9.1",
    "coveralls": "^2.11.1",
    "istanbul": "^0.2.10",
    "jshint": "^2.5.1",
    "mocha": "^1.20.0",
    "precommit-hook": "^1.0.2"
  },
  "jshintConfig": {
    "node": true
  },
  "keywords": [
    "cache",
    "localstorage",
    "lru"
  ]
}
